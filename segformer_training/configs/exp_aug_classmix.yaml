_base_: "base.yaml"

experiment:
  name: "exp_aug_classmix"
  tags: ["phase3", "augmentation", "classmix"]

augmentation:
  strategy: "classmix"
  random_scale:
    enabled: true
  color_jitter:
    enabled: true
  gaussian_blur:
    enabled: true
  classmix:
    prob: 0.5
    num_classes: null

training:
  report_to: "none"
